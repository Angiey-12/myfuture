import { AccessibleExternalLinksPipe } from './accessible-external-links.pipe';

describe('AccessibleExternalLinksPipe', () => {
  it('create an instance', () => {
    const pipe = new AccessibleExternalLinksPipe();
    expect(pipe).toBeTruthy();
  });
});
